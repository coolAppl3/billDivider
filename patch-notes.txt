Frontend:

  - ErrorSpan.js:
  
    - Both display() and hide() will not both stop and return undefined if the formGroup argument is falsy or not an instance of HTMLElement.
  
  - LoadingModal.js:

    - display() now first checks if a loading modal exists in the DOM before continuing. If one exists, it stops and returns undefined. If not, it continues normally.
  

  - Unit testing:

    - ErrorSpan.js passed unit testing.
    - FormCheckbox.js passed unit testing.
    - LoadingModal.js passed unit testing.
    - locateLoginToken.js passed unit testing.