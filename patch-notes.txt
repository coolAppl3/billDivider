Frontend:

  - messagePopup.js:

    - Slight adjustment to semantics.
  
  - createDateString.js:

    - The function now checks if the timestamp passed in is a type of number. If not, it will return undefined.
  
  - ConfirmModal.js:

    - _createBtnContainer() now falls back to 'cta' as the confirm button color, if a falsy argument is passed in.
    - _createConfirmMessage() now falls back to 'Are you sure?' as the confirmation message if a falsy argument is passed in.

  
  - Unit testing:

    - createDateString.js passed unit testing.
    - ConfirmModal.js passed unit testing.
