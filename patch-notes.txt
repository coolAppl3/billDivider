Frontend:

  - messagePopup.js:

    - The function now checks if the message argument passed in is of type string. If not, it will turn it into an empty string.
  

  - Unit testing:

    - messagePopup.js passed unit testing.